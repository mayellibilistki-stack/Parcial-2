#Base é a variavel para fazer a pergunta da base do triângulo
#Altura é a variavel para fazer a pergunta da altura do triângulo
base = float(input("Qual a base do triângulo: "))
altura = float(input("Qual altura do triângulo: "))
    
    # Depois, vem a forma que vai ser usado para calcular a área
area = (base * altura) / 2
    
    # E por fim mostra o resultado final
print(area)


